const dbconfig = {
  host: 'localhost',
  user: 'root',
  password: 'root',
  database: 'b2'
}

const fileconfig = {
  data: __dirname + '/../data/'
}

exports.dbconfig = dbconfig;
exports.fileconfig = fileconfig;